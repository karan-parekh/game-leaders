# Timed-out results are provisional

When a session times out, its latest scores remain resumable and may contribute provisionally to leaderboard views. Resuming replaces that snapshot, finalization makes the result permanent, and discard removes it from leaderboard consideration.
